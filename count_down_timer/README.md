<!-- #if (hours >= 0) {
          if (timer.minutes >0 && timer.hours >= 0){
            if (timer.seconds == 0 ){
              seconds = 59
              minutes = timer.minutes - 1
            }
            else {
              seconds = timer.seconds - 1
            }
            
          }
          else {
            minutes = 59
            hours= timer.hours - 1
          }
        } -->